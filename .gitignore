.gradle
/build/
.settings
.project
eureka-server/.settings
eureka-server/bin
eureka-server/build
eureka-server/.classpath
eureka-server/.project
hazelcast-separate-client/.settings
hazelcast-separate-client/bin
hazelcast-separate-client/build
hazelcast-separate-client/.classpath
hazelcast-separate-client/.project

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties
